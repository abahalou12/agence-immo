{% extends 'base.html.twig' %}

{% block title 'Ali_Immo' %}

{% block body %}
    <div class="jumbotron text-center">
        <h1>▂▃▇█▓▒░Bienvenue !░▒▓█▇▃▂</h1>
                    <h3>●▬▬۩Ali_Immo۩▬▬●</h3>
    <p>Lorem ipsum dolor sit amet, consectetur adipisicing elit. Ad adipisci animi beatae cumque cupiditate delectus
        deleniti illo, incidunt iste, molestias non nulla odit officiis porro recusandae, sint vel vitae voluptates.</p>
    </div>
    <div class="container">
        <h2>Les derniers biens</h2>
        <div class="row flex">
            {% for property in properties %}
                <div class="col-3">
                    <div class="card body">
                        <h5 class="card title">
                            <a href="{{ path('property.show', {id: property.id, slug: property.slug}) }}">{{ property.title }}</a>
                        </h5>
                        <p class="card-text">{{ property.city }} {{ property.postalCode }}</p>
                        <div class="text-primary" style="font-weight: bold; font-size: 2rem">{{ property.formatedPrice }} MAD</div>

                    </div>
                </div>
            {% endfor %}
        </div>
    </div>
{% endblock  %}